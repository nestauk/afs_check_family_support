@props(wrap: false, as: "div")
<{{ as }} {{ attributes.merge(
  class: "
    -:w-full -:max-w-full
    -:flex -:gap-2 sm:-:gap-4 -:items-center
    -:no-underline
    #{wrap ? "flex-wrap" : "flex-wrap sm:flex-nowrap"}
") }}>
  {{ slot }}
</{{ as }}>
