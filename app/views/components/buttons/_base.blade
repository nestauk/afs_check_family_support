<button {{ attributes.merge({
    "class": "
        px-4 pt-2 pb-1.5 rounded border-b-2
        active:translate-y-0.5 active:border-0
        select-none
        font-semibold
        focusable
    ",
    {{-- Prevent the button being focused on a click --}}
    "onmouseup": "this.blur()"
}) }}>
    {{ slot }}
</button>