@props(method: "POST")
<form {{ attributes.merge({
  class: "-:max-w-full -:flex -:flex-col -:gap-inherit",
  method: "POST",
}) }}>
  @method(method)
  <input type="hidden" name="authenticity_token" value="{{ form_authenticity_token }}"/>
  {{ slot }}
</form>