@props({title: _required})
<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    {!! csrf_meta_tags !!}
    {!! csp_meta_tag !!}

    {!! vite_client_tag !!}
    {!! vite_javascript_tag 'application' !!}
  </head>

  <body {{ attributes }}>
    <main class="max-w-5xl mx-auto mt-28 px-5 flex flex-col gap-4 sm:gap-6 pb-36 items-start">
      {{ slot }}
    </main>
  </body>
</html>
